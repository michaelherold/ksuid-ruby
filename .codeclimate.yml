engines:
  duplication:
    enabled: true
    config:
      languages:
        - "ruby"
  reek:
    enabled: true
  rubocop:
    enabled: true
    channel: rubocop-0-92

ratings:
  paths:
    - "Gemfile.lock"
    - "**.rb"

exclude_paths:
  - "spec/**/*.rb"
